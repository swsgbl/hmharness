fix: close the half-built gaps found in a full project audit

user-facing:
  - TUI slash palette: arrow keys now move the selection (scrolling
    6-row window, selected row marked with a pointer), Enter runs the
    highlighted command, Tab completes it - the palette was display-only
  - REPL: full slash-command set (same as the TUI palette: /tools /skills
    /ops [/ops scan] /bench /evolve /mcp /status /web /help), localized;
    previously only /exit existed
  - hmh --help / -h / help: usage output (previously fell into the REPL)
  - REPL stdin EOF hung forever (rl.question never settles after close);
    now races the close signal and exits cleanly in ~100ms
  - README.md: the Chinese README was mojibake in every commit since the
    repo went public (double-encoded at authoring time, invisible locally
    because local terminals also render it wrong) - rewritten clean,
    synced with the English one (workspaces, fullscreen TUI, --help,
    --locale), and the unfulfilled npx usage replaced by npm link
  - docs: ROADMAP - fullscreen TUI is fact (the old text said it was
    dropped), the two-frontend round is recorded, todos now carry
    blocked/queued markers, private-doc locations noted; ARCHITECTURE -
    memory distillation and context compression marked as implemented
  - evolution gate: candidates (and holdout re-verification) now require
    two independent passing samples per case - a single lucky output can
    no longer clear the promotion gate
